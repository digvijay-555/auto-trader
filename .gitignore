/target
/backend/target

# Runtime state — must never be committed (fights `git reset --hard` on deploy)
session.json
**/session.json
trades.db
trades.db-*
**/trades.db
**/trades.db-*

# Downloaded / installed server release binaries on the deploy host
backend/server-v*
backend/server/server-v*
backend/server/server-bin
backend/server/server-latest

.env

# VSCode extension artifacts — Next.js snippet cheatsheets dropped into docs/.
# Unrelated to this project (Vite/React, not Next.js); ignored so a regenerated
# copy cannot slip into a commit.
docs/javascript-snippets.md
docs/typescript-snippets.md

# Private keys / credentials
*.pem
auto-trader-key.pem
